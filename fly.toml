# fly.toml app configuration file generated for pishiqbot on 2026-09-18T05:08:04Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'pishiqbot'
primary_region = 'ams'

[build]

[deploy]
  release_command = 'alembic upgrade head'

[env]
  TIMEZONE = 'Asia/Tashkent'

[processes]
  app = 'python -m app.main'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[restart]]
  policy = 'always'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
